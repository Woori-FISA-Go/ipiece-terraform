################################################################################
# 1. Terraform 코어 파일 (상태/모듈)
################################################################################
# 로컬 모듈 및 공급자(provider) 캐시
.terraform/

*.tfstate
*.tfstate.*.backup

# Terraform 크래시 로그
crash.log

################################################################################
# 2. 변수 파일 (Secrets)
################################################################################
*.tfvars
*.tfvars.json

################################################################################
# 3. Plan 파일 (Artifacts)
################################################################################
# 'terraform plan' 실행 계획 파일 (민감 정보 포함 가능)
*.tfplan

# 'terraform show -json' 출력 파일 (Infracost용 중간 파일)
# (이 파일들은 소스 코드가 아닌 생성된 결과물입니다)
*.plan.json
plan.json
tfplan.json

################################################################################
# 4. Infracost 파일 (Cache)
################################################################################
# Infracost 로컬 캐시 및 설정
.infracost/

# Infracost 실행 결과물 (사용자 목록 기반)
infracost-base.json
baseline.json
plus.json

################################################################################
# 5. OS 및 기타 파일
################################################################################
# macOS
.DS_Store

# Windows
Thumbs.db

# 일반 로그 파일
*.log